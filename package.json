{
  "name": "robin",
  "version": "0.2.0",
  "license": "MIT",
  "browserslist": ">1% in ch",
  "engines": {
    "node": ">=16"
  },
  "devDependencies": {
    "browser-sync": "^2.27.10",
    "esbuild": "^0.15.0",
    "esbuild-plugin-eslint": "^0.0.1",
    "esbuild-postcss": "^0.0.4",
    "eslint": "^8.21.0",
    "eslint-config-sweet": "^8.0.1",
    "postcss": "^8.4.16",
    "postcss-preset-env": "^7.7.2",
    "stylelint": "^14.9.1",
    "stylelint-config-rational-order": "^0.1.2",
    "stylelint-config-standard": "^26.0.0",
    "stylelint-order": "^5.0.0"
  },
  "scripts": {
    "start": "node build.mjs --watch",
    "prod": "node build.mjs --minify --watch",
    "build": "node build.mjs --minify"
  },
  "dependencies": {
    "cosha": "^2.0.0"
  }
}
